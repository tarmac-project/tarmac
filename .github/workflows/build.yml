name: build

on:
  push:
    tags:
      - v*
    branches:
      - master
      - main
  pull_request:
jobs:
  tests:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: bitnami/mysql:latest
        env: 
          MYSQL_ROOT_PASSWORD: example
          MYSQL_DATABASE: example
        ports:
          - 3306:3306
      redis:
        image: bitnami/redis:latest
        # Set health checks to wait until redis has started
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        env:
          ALLOW_EMPTY_PASSWORD: yes
      consul:
        image: bitnami/consul:latest
        ports:
          - 8500:8500
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -skip "TestFullService/.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3

  redis:
    runs-on: ubuntu-latest
    services:
      redis:
        image: bitnami/redis:latest
        # Set health checks to wait until redis has started
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        env:
          ALLOW_EMPTY_PASSWORD: yes

      redis-sentinel:
        image: bitnami/redis-sentinel:latest
        env:
          REDIS_URL: redis://redis:6379
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -run "TestFullService/Redis.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3

  cassandra:
    runs-on: ubuntu-latest
    services:
      cassandra-primary:
        image: madflojo/cassandra:latest
        env:
          CASSANDRA_KEYSPACE: tarmac
        ports:
          - 7000
          - 7001
          - 7199
          - 9042
          - 9160

      cassandra:
        image: madflojo/cassandra:latest
        env:
          CASSANDRA_SEEDS: cassandra-primary
          CASSANDRA_KEYSPACE: tarmac
          SLEEP_TIMER: 15
        ports:
          - 7000
          - 7001
          - 7199
          - 9042
          - 9160
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -run "TestFullService/Cassandra.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3


  boltdb:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -run "TestFullService/BoltDB.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3


  in-memory:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -run "TestFullService/In-Memory.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3


  mysql:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: bitnami/mysql:latest
        env: 
          MYSQL_ROOT_PASSWORD: example
          MYSQL_DATABASE: example
        ports:
          - 3306:3306
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -run "TestFullService/MySQL.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3

  postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: bitnami/postgresql:latest
        env: 
          POSTGRESQL_USERNAME: example
          POSTGRESQL_PASSWORD: example
          POSTGRESQL_DATABASE: example 
        ports:
          - 5432:5432
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: '1.21'
    - name: Build Test WASM Modules
      run: make build
    - name: Execute Tests
      run: |
        go test -v -race -covermode=atomic -coverprofile=coverage.out -run "TestFullService/Postgres.*" ./...
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3 
